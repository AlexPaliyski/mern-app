import '../styles/WatchlistButton.css'
import {Button} from "react-bootstrap";

function WatchlistButton(){
    return(
        <Button variant={'primary'} className={'mx-4 gradient-button'}>Your Watchlist</Button>
    )
} export default WatchlistButton;